
- name: Setting up webservers
  hosts: webservers
  become: true
  roles: 
    - nginx_setup
  tasks:
  - name: Ping available hosts
    ping:

  - name: Add webadmin users
    become: yes
    user: 
      name: webadmin
